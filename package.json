{
  "name": "v6_proxy",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ v6_proxy",
    "create-local": "graph create --node http://localhost:8020/ v6_proxy",
    "remove-local": "graph remove --node http://localhost:8020/ v6_proxy",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 v6_proxy",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.71.2",
    "@graphprotocol/graph-ts": "0.32.0",
    "envio": "^1.3.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
